# RP-08 — Functionality Audit, UX and Accessibility Review, and Improvement Backlog

## Metadata

| Field | Value |
| --- | --- |
| Report id | RP-08 |
| Date | 2026-08-20 |
| Subject | Group Lesson Planner, https://denyslystopadskyy.github.io/lesson-planner/ |
| Source of truth for citations | `index.html` at commit `211eb6b`, 1473 lines, confirmed by RP-01 to be byte-identical to the deployed artifact |
| Inputs consumed | research agent contract; verified-facts addendum; RP-01 in full; RP-05 section 6; `index.html` read in full |
| Accessibility standard applied | WCAG 2.1, criterion numbers and conformance levels taken from the W3C Recommendation [S1] |
| Runtime environment | Playwright-driven Chromium 151 on macOS against the live HTTPS origin; viewports 1200x897, 1280x900, 768x1024, 375x812 |
| Origin hygiene | `localStorage` key set recorded before any write: **empty**. Teardown verified: empty again, and the first-run empty state re-renders. `sessionStorage` was empty throughout and was never written |

**Verification statement.** The keyboard sweeps, first-run state, accessibility tree, label and accessible-name audit, all colour values and every contrast ratio, all viewport geometry at 375 px and 768 px, the seeded 3-year dataset and its render timings, the `localStorage` capacity probe and `navigator.storage.estimate()` result were **runtime-verified** this session on the live origin. Every contrast ratio in section 3 was **recomputed by this agent** from runtime-resolved colours using the WCAG relative-luminance formula; none was copied from RP-01. Line numbers, string literals, the absence of features proven by whole-file greps, and the default-template composition were **statically read**. WCAG criterion numbers and levels, the ARIA patterns, Web Share constraints, Web Storage limits, iCalendar and the comparable-tool capabilities were **externally sourced** and are cited. Anything reasoned rather than observed is labelled inference inline. RP-01's 26 defects are cited, not re-reported.

## Executive summary

The application does one job well: it turns a painted monthly calendar into a fee message on the clipboard, in about 200 ms, with zero dependencies. Nothing in this audit argues for replacing that shape.

Six conclusions dominate.

1. **Performance is not a problem and never will be at this scale.** A modelled three years of use — 8 groups, 36 months, 2,304 lesson dates — occupies 70,361 bytes, which is about 1.4 % of the `localStorage` ceiling I measured directly on this origin: `QuotaExceededError` on the write after 5,177,344 characters had been stored successfully in one key. Every render completes in under 60 ms. **RP-01's quota TBD is closed**, with the unit caveat in section 4 M9. Do not spend effort here.
2. **What degrades with use is ergonomics, not speed.** At three years of history the group modal renders 36 month rows sorted oldest-first, so the row she needs every single month is row 36 of 36 — 1,381 px of scrolling on a desktop and 2,627 px on a phone, and it is not visible when the modal opens. This is the single largest recurring time cost in the product and it gets worse every month she uses it.
3. **The app keeps no record of what it has sent.** `copyAndClose` (`index.html:901-910`) writes to the clipboard and stores nothing; greps for `sentAt`, `history` and `archive` return zero. Across 36 visually identical month rows there is no way to tell an invoiced month from an uninvoiced one. For a monthly billing task, that is a capability gap, not a nicety.
4. **Money can go wrong quietly, and the wrongness reaches students.** The price inputs carry no `min` or `step`, so a negative price produces a real generated message reading `total fee of -PLN 50.00` — runtime-verified. The shipped default template's own wording produces `we will have 1 lessons` for any single-lesson month; RP-01 rated that defect Low on the strength of two internal UI strings, but the same wording is in the outgoing message. And at three years of history one keystroke in the bulk price box rewrote **36 months** of prices in the draft — measured, not inferred.
5. **The WCAG version matters and the brief's framing needs correcting.** Under WCAG 2.1 the only target-size criterion is 2.5.5 Target Size at **Level AAA**, requiring **44x44 CSS px** [S1]; there is no target-size requirement at AA. RP-01 judged target size *passing* against 2.5.8 Target Size (Minimum) at 24x24, which is a WCAG **2.2** criterion confirmed absent from 2.1 [S1]. Both reports are right about their own version. Measured here: day cells 41.6x29 px on a phone, toolbar buttons about 29.5 px tall — an AAA gap, not an AA failure. The genuine AA failures are contrast (1.4.3, 1.4.11), status messages (4.1.3), language of parts (3.1.2) and label quality (2.4.6); the genuine Level A failures are keyboard operability (2.1.1), focus order (2.4.3), name/role/value (4.1.2), labels (3.3.2) and semantics (1.3.1).
6. **For this specific user, accessibility is the fourth priority, and I will say so.** She is one sighted teacher using a mouse. The keyboard and screen-reader failures are real, cheap to fix during the migration, and block RP-03's Playwright suite — but they do not cost her time today. The three changes that do are ranked in section 7.

Three findings are new and were not in RP-01: the group-name prefill collides with the first thing she types; the phone-clipped `◀` button is unreachable **even with scrolling**, not merely inconvenient; and the app's one deliberate focus indicator fails 1.4.11 at 2.78:1.

## 1. Feature coverage audit

Verdicts are against the RP-01 section 2 inventory as baseline. Absences are proven by whole-file greps, not asserted.

| Capability | Verdict | Evidence | Relevance to this user |
| --- | --- | --- | --- |
| Message templating with variable substitution | Partial | Three tokens `{{month}}`, `{{lessons}}`, `{{total}}` substituted by regex at `index.html:1374-1377`. Runtime-verified: correct tokens fully substitute, no braces remain. But the template is a **single global string** (`index.html:1207-1211`), not per group | Core to her job. The global scope is the concrete limit: a UAH group and a PLN group must share one bank-details block |
| Token coverage | Partial | No token for group name, per-lesson price, date list, due date or teacher name. A mistyped token such as `{{Month}}` passes through silently | She cannot name the group in the message she sends to that group |
| Tone or formality variants | Absent | One template value only. No named variants, no reminder or final-notice form | A second, sterner reminder must be hand-edited in the review textarea every time and cannot be kept |
| Student records | Absent | Zero occurrences of `student` in the file. No student entity in the data model | Correct by design. She addresses a group chat, not individuals. It does mean no per-recipient personalisation is possible at all |
| Group records | Present, identity-less | `name`, `price`, `currency`, `dates`, `monthlyOverrides` created at `index.html:660-666`. Identity is the array index (`604`, `1044`); CSV import re-keys by name (`1323`) | Adequate today. The index identity is what makes an undo or a sync layer awkward later |
| Per-group currency | Present | `group.currency` drives every format call (`1091`, `1379`). Only the *default* is a global singleton (`683`, `1358`) | She offers UAH and PLN; the per-group part already works |
| Reusable lesson content and objectives | Absent | Zero occurrences of `notes` or `objective`. A lesson is a bare `YYYY-MM-DD` string | Not needed. The app's output is a fee message; content would never appear in it |
| Scheduling | Present | Calendar painting (`783-791`), per-month grouping (`1218-1226`), per-month totals (`1080-1101`) | The product's core, and it works |
| Recurrence | Partial | Weekday bulk-select (`797-814`) applies to the visible month only. Zero occurrences of `recurr`: no rule is persisted | A fixed weekly pattern must be re-painted in every month, twelve times a year |
| Sent-message history | Absent | Zero occurrences of `sentAt`, `history`, `archive`. `copyAndClose` (`901-910`) records nothing | Real cost. Verified at 36 month rows: nothing distinguishes an invoiced month |
| Search and filtering | Absent | Zero occurrences of `Search` or `Filter`. The five `filter` hits are `Array.prototype.filter` and a CSS `filter`. Cards are auto-sorted by name (`1029-1033`) with no control | Not needed for a handful of groups. The list that needs taming is month rows, not groups |
| Bilingual or multi-language output | Partial, by accident | The default template block (`index.html:382-400`) is mixed English and Ukrainian. Runtime-verified: the document contains exactly **one** `lang` attribute, `<html lang="en">` at line 2. All three format calls pinned `en-US` (`1373`, `1380`, `1386`); month names from a hardcoded English array (`379`) | She writes to Ukrainian-speaking students from Poland. There is content in two languages but no language *mechanism* |
| Export: clipboard | Present | `navigator.clipboard.writeText` at `index.html:902`, secure context confirmed | This is the right primary export for her channel |
| Export: CSV file | Present | `saveToCsv` at `915-931`, download verified by RP-01 | Works, but see the backup verdict below |
| Export: email | Absent | Zero occurrences of `mailto:` | No email path exists |
| Export: messaging apps | Absent | Zero occurrences of `wa.me`, `whatsapp`, `telegram`, `t.me`, `navigator.share` | On a phone she must leave the app, find the chat and paste |
| Export: PDF or print | Absent | Zero occurrences of `pdf` or `window.print` | Not needed for a chat-pasted message |
| Export: JSON backup | Absent | Zero occurrences of lowercase `json` outside `JSON.stringify` internals | The gap RP-05 section 6 closes first |
| Backup completeness | Partial and unsafe | CSV omits `paymentTemplate` (RP-01 D16), has no BOM (D17), throws on a malformed month key (D4), and silently destroys existing data on a balanced stray quote (D12) | The only restore path is the one least able to be trusted |
| Autosave | Partial | `save()` is called from `saveGroup` (`684`), `deleteGroup` (`712`), `saveDateChanges` (`764`), `loadFromCsv` (`948`). `updateDefaultPrice` (`690-705`) mutates live state without persisting. Per the addendum, `handleSelectedDatesPriceChange` correctly writes to the draft and is not a defect | Nothing persists while she paints dates; a crash mid-edit loses the session |
| Undo | Absent | Zero occurrences of `Undo`. The single `undo` hit is the literal string in the clear-all confirmation | Delete Group, CSV import and Clear All Data are all irreversible |
| Error recovery: storage | Absent | No `try`/`catch` anywhere in the storage service (`1187-1213`). RP-01 D14 runtime-verified a silently dead page | The most plausible mechanism for a repeat of the original data loss |
| Error recovery: input validation | Absent | Runtime-verified: `min`, `max`, `step`, `required` and `maxlength` are all null on `#groupNameInput`, `#groupPriceInput`, `#yearInput` and `#selectedDatesPriceInput` | Direct cause of the negative-price message and of RP-01 D4 |
| Money arithmetic and formatting | Present | Lessons times price per month, `Intl.NumberFormat` (`1379-1381`). Verified `PLN 0.00`, `-PLN 100.00` | Correct, and the only thing in the app she cannot check by eye |

## 2. UX findings

Severity is judged by the brief's test: does it cost her time, lose her work, or embarrass her in front of students and parents.

| Finding | Severity | Evidence | Proposed fix |
| --- | --- | --- | --- |
| U1 The current month is always the last of N month rows and off-screen on open | High | Seeded 36 months: rows sorted oldest-first at `index.html:1067`, `August 2026` is row 36 of 36, needs 1,381 px of scrolling at 1280x900 and 2,627 px at 375x812, `currentMonthInitiallyVisible: false` at both | Sort newest-first, expand only the current month, collapse earlier months behind a toggle, and scroll it into view on open |
| U2 No record of what has been sent | High | Nothing written on `copyAndClose` (`901-910`); greps for `sentAt`, `history`, `archive` all zero; 36 rows visually identical apart from numbers | Store a `sentAt` timestamp per group-month and render it in the row |
| U3 One keystroke in the bulk price box rewrote 36 months | High | `startEditingDates` loads every date into the draft (`720`); the seeded draft held 289 dates; `handleSelectedDatesPriceChange` (`858-881`) applied one typed value to **36** month keys — measured `monthsRewrittenByOneKeystroke: 36`. Extends RP-01 D3 with a scale-dependent blast radius | Scope the write to the visible month, or state the count and require confirmation before writing more than one month |
| U4 Typing a group name collides with the literal prefill | High | Runtime-verified: `#groupNameInput.value` is `Untitled Group`, focus lands with selection `[14,14]` (caret at end, nothing selected). Typing `B2 Adults` yields `Untitled GroupB2 Adults`. Source: `index.html:614`, `635` | Use a `placeholder` instead of a value, or call `select()` after `focus()` |
| U5 A negative or fractional price flows into the outgoing message | Medium | Generated line runtime-verified: `In August, we will have 1 lessons, with a total fee of -PLN 50.00.` `min` and `step` are empty strings on `#groupPriceInput` | `min="0"`, `step="0.01"`, plus a clamp in the handler |
| U6 The plural defect is in the message students read, not just the UI | Medium | Verified generated line `we will have 1 lessons`. The wording lives in the shipped default template at `index.html:384`, so it reaches anyone who has not customised the template. RP-01 D19 rated this Low on two internal strings only | Fix the two UI strings and the default template wording together |
| U7 The monthly return-visit workflow is six steps, three of them navigation | Medium | Observed: open app, click card, scroll to the current month, click Copy Payment Message, read or edit, Copy and Close. Two of the six produce nothing but position | Surface a `Copy this month` action on the group card so the common case is two clicks |
| U8 Delete Group sits next to Done, in tab order and on screen | Medium | Tab order while the schedule editor is open: `cancelDateChangesBtn`, `saveDateChangesBtn`, `deleteGroupBtn`. The 375 px screenshot shows Delete Group directly below Done | Move Delete into the info-edit form, or require the group name to be typed |
| U9 Focus is destroyed on every save | Medium | Runtime-verified: after Enter on `#saveGroupBtn`, `document.activeElement` is `BODY`. Cause is the `innerHTML` rebuild at `1019` | Restore focus to a stable anchor after re-render |
| U10 First run offers no orientation and no warning about where data lives | Medium | The only first-run guidance is `No groups yet. Click '+ Add Group' to get started!`. Nothing states that data exists only in this browser — which is exactly how it was lost | One line of first-run text plus the backup-age indicator from RP-05 section 6 |
| U11 Destructive and no-op toolbar actions are as prominent as the primary action at zero data | Medium | First-run snapshot: all five toolbar buttons enabled with no data. `Save CSV` alerts `There are no groups to export yet.` (`917`); `Clear All Data` prompts and then clears nothing | Disable `Save CSV` and `Clear All Data` while `groups.length === 0` |
| U12 CSV import replaces everything with no confirmation | High | RP-01 D11, cited not re-reported. Worth restating only because it guards the app's sole restore path | Confirm, and show how many groups will be replaced |
| U13 `beforeunload` fires on every navigation and its advice is never shown | Low | Re-verified: dialog fires with an empty message; browsers discard custom text | Remove it once autosave gaps and the backup nag exist |
| U14 Where the app is already good enough | n/a | One document request, ~200 ms load, zero dependencies; the draft-then-commit pattern for schedule edits is sound; clipboard is the right endpoint for a chat-pasted message; per-group currency works; the card grid and numeric-aware sort need nothing; **768 px tablet has no layout break at all** | No action |

On U1 and U7 together: the monthly task is the whole product, and both findings are about the distance between opening the app and having the message on the clipboard. Fixing them is the only change in this report whose benefit grows every month she keeps using the tool.

## 3. Accessibility findings

Criterion numbers and levels are from the WCAG 2.1 Recommendation [S1]. Every ratio below was computed by this agent from runtime-resolved colours; the 4.5:1 and 3:1 thresholds and the large-text definition (18 pt = 24 px, or 14 pt bold = about 18.5 px) are from SC 1.4.3 [S2], and the 3:1 non-text threshold including focus and state indicators is from SC 1.4.11 [S3].

| Finding | WCAG criterion | Severity | Proposed fix |
| --- | --- | --- | --- |
| A1 The 31 day cells and 7 weekday headers are non-focusable `div`s, so there is **no keyboard route to select a lesson date at all**. Verified: `tabIndex: -1`, no `role`, no key handler; the only focusables inside `#calendar-container` are 9 chrome controls, and `#selectedDatesPriceInput` leaves the tab order while disabled, so a fresh editor exposes 8 | 2.1.1 Keyboard (A) | Critical | Adopt the ARIA grid pattern: `role="grid"`, one tab stop, arrow keys between cells, Enter or Space to toggle [S4] |
| A2 Group cards are non-focusable `div`s (`tabIndex: -1`, `role: null`, `hasKeydown: false`, `hasClick: true`), so on a **return visit** no group can be opened by keyboard. A 16-stop sweep with a card present never reaches it. Nuance worth recording: a keyboard user *can* reach `#editScheduleBtn` in the same modal session immediately after creating a group, so the block is on returning, not on first use | 2.1.1 Keyboard (A) | Critical | Render the card as a `button`, or add `role="button"`, `tabindex="0"` and an Enter/Space handler |
| A3 Closed modals stay focusable and in the accessibility tree: independently re-verified as **9 of 14 tab stops**, all three closed overlays computing `visibility: visible` | 2.4.3 Focus Order (A), 4.1.2 Name Role Value (A) | High | Add `hidden` plus a `[hidden] { display:none !important }` rule; the developer's uncommitted copy already does this |
| A4 No focus trap. Tabbing out of the **open** group modal continues into the closed review modal and then to the page toolbar (`#addGroupBtn`, `#editTemplateBtn`) — background controls the overlay hides from the mouse | 2.4.3 Focus Order (A) | High | Contain Tab and Shift+Tab within the open dialog [S5] |
| A5 No `role="dialog"`, no `aria-modal`, no accessible name on any of the four dialogs. Verified counts: `role=` 0, `<dialog` 0, `aria-modal` 0 | 4.1.2 Name Role Value (A) | High | `role="dialog"`, `aria-modal="true"`, `aria-labelledby` pointing at the existing heading [S5] |
| A6 Focus is not restored when a dialog closes, and is lost outright on save (`activeElement` becomes `BODY`) | 2.4.3 Focus Order (A) | Medium | Return focus to the invoking control [S5] |
| A7 Five form controls have no accessible name: `#templateTextarea`, `#reviewTextarea`, `#monthSelect`, `#yearInput`, `#csvInput`. The two textareas are where the payment message itself lives | 4.1.2 (A), 3.3.2 Labels or Instructions (A) | High | Add `<label for>` or `aria-label` to each |
| A8 Three `<label>` elements label nothing — no `for`, no wrapped control — at `index.html:267`, `271`, `275`, so the read-only Group Name, Default Price and Currency values have no programmatic association | 1.3.1 Info and Relationships (A) | Medium | Use a description-list or `aria-labelledby` pairing instead of orphan labels |
| A9 At 36 month rows, **36 buttons share the identical accessible name** `📋 Copy Payment Message`. Also 4 buttons named `Cancel` and 2 named `Save` | 2.4.6 Headings and Labels (AA), 4.1.2 (A) | High | Include the month in the name, e.g. `Copy payment message for August 2026`. This also unblocks RP-03's role-based queries |
| A10 Zero landmarks, and the heading outline exposes two invisible dialog headings. Verified outline: H1, 8x H2 (cards), H3 (closed template modal), H3 (open group modal), H4, H3 (closed review modal) | 1.3.1 Info and Relationships (A) | Medium | Add a `main` landmark; fixing A3 removes the phantom headings |
| A11 No status message is announced. Verified `aria-live` count 0. Affects `#calendar-summary` (`1171-1173`) and the `Copied!` label (`905`), which is the **only** confirmation that the copy succeeded | 4.1.3 Status Messages (AA) | Medium | `role="status"` on the summary and on a copy-confirmation region [S6] |
| A12 Text contrast failures against the 4.5:1 minimum: white on accent `#4caf50` at 13.33 px (primary buttons) = **2.78:1**; white on `#4caf50` at 16 px weight 600 (selected day cells, below the 18.5 px large-text floor) = **2.78:1**; `#ef4444` on white at 13.33 px (`.danger`) = **3.76:1**, worsening to **3.08:1** on its own `#fee2e2` hover background; `#94a3b8` at 11 px on `#f8fafc` (`.input-helper`, `.price-per-lesson`) = **2.45:1** | 1.4.3 Contrast Minimum (AA) | High | Darken the accent and the danger colour; drop the 11 px muted grey or darken it |
| A12b Text contrast passes worth stating, because the app is already fine here: `#64748b` on card white = **4.76:1**; `#64748b` on `#f8fafc` = **4.55:1**; `#0f172a` on `#f8fafc` = **17.06:1** and on white = **17.85:1**; black on white = **21:1**; `#475569` on `#e2e8f0` icon hover = **6.15:1** | 1.4.3 (AA) | none | No action |
| A13 The app's **one deliberate focus indicator fails**: `.icon-button:focus` uses `outline:2px solid var(--accent)` (`index.html:134`), and `#4caf50` against white is **2.78:1** against the 3:1 required for focus indicators [S3] | 1.4.11 Non-text Contrast (AA) | High | Use a dark or high-contrast ring, and apply it to every control rather than one icon button |
| A14 Other non-text contrast failures: selected-day fill `#4caf50` against the adjacent unselected surface `#f8fafc` = **2.66:1**; `.today` border, same pair = **2.66:1**; card border `#e2e8f0` on body `#f8fafc` = **1.18:1** and card fill on body = **1.05:1**, so cards have essentially no boundary; day-cell border, same pair = **1.18:1**; and **button border `#ccc` on white = 1.61:1**, which is every toolbar and action button outline — a pair RP-01 did not measure. Passing: the UA input border `rgb(118,118,118)` on white = 4.54:1 | 1.4.11 Non-text Contrast (AA) | High | Darken borders to at least 3:1; give the selected state a second cue |
| A15 Colour is the only channel for selection, for today, and for weekends — and the weekend cue **does not exist**: `.weekend` resolves to `rgb(248,250,252)` on a container resolving to the identical `rgb(248,250,252)`, exactly **1.00:1**, confirmed in a 375 px screenshot | 1.4.1 Use of Color (A) | Medium | Add a glyph or border weight to the selected state; give weekends a real tint or delete the rule |
| A16 The document's only `lang` attribute is `<html lang="en">`, but the default template and therefore the generated message contain Ukrainian text | 3.1.2 Language of Parts (AA) | Low for her, real for conformance | Mark the Ukrainian block, or store a language per template |
| A17 No error identification, no input constraints, and destructive actions with no reversal on a financial workflow: CSV import replaces all data unconfirmed, and the bulk price rewrites unseen months | 3.3.1 Error Identification (A), 3.3.2 (A), 3.3.4 Error Prevention Legal Financial Data (AA) | High | Constrain the inputs, confirm the import, scope or confirm the bulk write |
| A18 Target size. Measured: day cells **41.6x29** px at 375 px and 79.4x29 at 1280 px; weekday headers 45x27; toolbar buttons about 29.5 px tall. Under WCAG 2.1 the only target-size criterion is 2.5.5, **Level AAA, 44x44 CSS px** [S1] — there is **no target-size requirement at AA** — so these are AAA gaps. RP-01's "passes" was measured against 2.5.8 Target Size (Minimum) at 24x24, a WCAG **2.2** criterion confirmed absent from 2.1 [S1] | 2.5.5 Target Size (AAA) | Low against the AA brief, Medium as phone ergonomics | Raise day-cell and button height to 44 px on touch widths; this is a usability change more than a conformance one |

Two notes on what I deliberately did **not** claim. First, 2.4.7 Focus Visible (AA) is arguably satisfied for the app's real controls, because Chromium's default focus ring is visible on every native `button`, `input` and `select`; the defect is that the *custom* ring fails 1.4.11 (A13) and that the `div`-based controls cannot show focus at all because they never receive it (which is A1 and A2, not 2.4.7). Second, the `alert()` calls on CSV failure are native dialogs that do take focus, so they are outside 4.1.3's scope [S6]; the un-announced changes are the calendar summary and the copy confirmation.

## 4. Mobile and performance findings

| Finding | Measurement | Verdict |
| --- | --- | --- |
| M1 At 375 px the `◀` previous-month button is clipped off the left edge and **cannot be scrolled into view** | `.calendar-controls` computes `flex-wrap: nowrap` with `justify-content: center`; `scrollWidth` 364 against `clientWidth` 315. `#prevMonthBtn` renders at x = -19.1 with width 35.3, so 16.2 px of 35.3 is on screen. Setting `modal.scrollLeft = -9999` leaves it at 0; setting it to 9999 gives 19 and pushes the button to x = -38.1 | **Refines RP-01**, which said both clipped controls were reachable by scrolling inside the modal. The left one is not. The 16.2 px sliver is hit-testable (`elementFromPoint` returns `prevMonthBtn`) but is a 16x29 target on a phone |
| M2 The modal overflows horizontally at 375 px | `scrollWidth` 394 against `clientWidth` 375, giving a 19 px horizontal scrollbar inside the dialog. Page-level `scrollWidth` stays 375 | A page-level overflow check misses this entirely, which is why it reads as "no overflow" from outside |
| M3 `Clear Month`, the only destructive calendar control, is truncated and overhangs the viewport at 375 px | Right edge at x = 394.1 in a 375 px viewport; label width 59 px against 94.6 px at desktop | It is also easier to hit than `◀`, which is the wrong way round |
| M4 Tablet width has no layout break | 768x1024: `.calendar-controls` `scrollWidth` equals `clientWidth` at 580; modal 640 wide with no overflow; day cells 79.4x29; page `scrollWidth` 768 | **Already good enough. No action for tablet** |
| M5 `#calendar-summary` clips its own text at every width | `height: 1em` at `index.html:199` gives `clientHeight` 12 against `scrollHeight` 14 at both 375 px and 768 px | RP-01 D21, re-verified |
| M6 The phone toolbar consumes 128.5 px before any content | `h1` height 128.5 px at 375 px (five buttons wrapping to four rows) against 64.5 px at 768 px | Not broken, but the first ~148 px of a phone screen is chrome |
| M7 Touch target heights | Day cell 41.6x29 (1,206 px area) at 375 px; toolbar buttons about 29.5 px tall | See A18. AAA gap under WCAG 2.1, and genuinely fiddly on a phone |
| M8 **Load and render performance is a non-issue** | Seeded 8 groups x 36 months x 8 lessons = 2,304 dates, then reloaded: `domContentLoaded` 191 ms, `loadEventEnd` 200 ms, `JSON.parse` 1.3 ms, `render.groups()` 0.6 ms, `openGroupModal` 51.9 ms, `render.monthlyOverrides()` 10.6 ms for 36 rows, `startEditingDates` 8.6 ms, `render.calendar()` 0.2 ms, `toggleDateSelection` 0.2 ms, `storage.save()` 9.0 ms, 461 to 497 DOM nodes total | Nothing here needs optimising. Full destroy-and-rebuild rendering is fine at this scale |
| M9 **Storage headroom is ample. RP-01's quota TBD is closed, with a unit caveat** | Direct probe on the live origin, growing one key until it failed: the largest value written successfully was **5,177,344 JS characters**, which is **4.94 MiB measured in characters** and **9.88 MiB measured as UTF-16 bytes**; the next 64 KiB write threw `QuotaExceededError`. Probe key removed and verified gone. MDN states that Web Storage "is limited to 10 MiB of data maximum on all browsers" and that browsers "can store up to 5 MiB of local storage, and 5 MiB of session storage per origin", **without stating whether those figures count characters or bytes** [S7]. The measurement agrees with the 5 MiB figure read as characters, and contradicts it read as bytes | Volume will never be the problem: the 3-year dataset is 70,361 bytes, under 1.4 % of the ceiling on the least favourable reading. The absence of quota *handling* still is a problem (RP-01 D14) |
| M9b The ceiling is an origin budget shared across keys, not a per-key allowance | The probe grew a single key, so it establishes what one key can hold, not what three or four can hold simultaneously. The app uses three keys today and backlog item B22 proposes a fourth | Immaterial at 70 KB, but state it so nobody treats the figure as a per-key allowance when adding a snapshot key |
| M10 `navigator.storage.estimate()` is the wrong instrument for this question | Returned `quota` 10,737,418,240 bytes with `usage` 0 **while `localStorage` held data**, so Web Storage is not accounted for in that figure on this build. `navigator.storage.persisted()` returned `false` | Record this so no later report cites 10 GiB as the localStorage ceiling |
| M11 What actually degrades at scale is ergonomics and blast radius | 36 month rows oldest-first with the current month last and 2,627 px away on a phone; 36 identically named copy buttons; one keystroke rewriting 36 months of prices | This is the real "large dataset" finding. See U1, U3, A9 |

## 5. Comparable-tools gap analysis

Sources are product documentation and specifications. No capability is proposed because a competitor has it; every "worth adopting" verdict names the cost observed in this app's own workflow.

| Capability | Seen in | Adopt | Reason tied to observed cost |
| --- | --- | --- | --- |
| Repeating-lesson scheduling with a stored pattern | Teachworks documents repeating lessons with flexible repeat options [S10]; RFC 5545 defines `RRULE`, e.g. `FREQ=WEEKLY;BYDAY=MO,WE` [S8] | Yes, light form | The weekday bulk-select at `index.html:797-814` applies to the visible month only and stores no rule, so a fixed weekly pattern is re-painted twelve times a year. Adopt a generator that applies a weekday pattern across a chosen month range in one action. Do **not** adopt a stored recurrence engine — see section 8 |
| A record that an invoice or statement was issued | Teachworks documents automatic invoice generation, transaction history and overdue reminders [S11] | Partial | Adopt only a `sentAt` stamp per group-month. Observed cost: across 36 identical month rows nothing marks an invoiced month. Reject statuses, ledgers and reminders: there is no payment data in this app to reconcile against |
| Merge fields beyond three tokens | Gmail mail merge documents `@firstname`, `@lastname`, `@fullname`, `@email` plus spreadsheet-column tags [S9] | Yes, narrow | Observed cost: one global template (`1207-1211`) with no group token means the message she pastes into a group chat never names that group, and a UAH group and a PLN group share one bank-details block. Add `{{group}}`, `{{price}}`, `{{dates}}` |
| Multiple named template variants (first notice, reminder, final notice) | Teachworks documents custom email templates per scenario [S11] | Yes | Observed cost: a sterner reminder must be hand-edited in the review textarea every month and the edit cannot be kept, because there is exactly one template value |
| Lesson notes and objectives attached to a lesson | Teachworks documents lesson notes saved and emailed to students [S10] | No | Greps confirm no such field exists, and nothing in the observed workflow consumes one. The app's output is a fee message; notes would never appear in it. Adding them roughly doubles the data model and the modal for zero effect on the task |
| Calendar visible outside the tool | Teachworks documents calendar feeds to other devices [S10]; RFC 5545 defines `VEVENT` [S8] | Yes, as a one-way `.ics` download | Observed cost: her schedule exists only inside a modal in this app, which is not what she checks before leaving the house. A static `.ics` download needs no server. A **subscribable** feed does, and GitHub Pages cannot serve a per-user one — see section 8 |
| OS share sheet as an export path | `navigator.share()` is documented with a secure-context and transient-activation requirement and accepts a `text` field [S12] | Yes, as a progressive enhancement | Observed cost: the flow ends at the clipboard, so on a phone she leaves the app, finds the chat and pastes. Availability is explicitly not Baseline [S12], so it must sit behind a capability check with the existing clipboard path as fallback |
| Online payment collection | Teachworks documents Stripe card payments and automatic payment recording [S11] | No | See section 8 |
| Automatic email or SMS delivery | Teachworks documents emailed invoices and reminders [S11] | No | There are no recipient records at all (zero occurrences of `student`) and no backend to send from. See section 8 |
| PDF invoice generation | Teachworks documents PDF invoices [S11] | No | She pastes text into a chat. A PDF is a worse artefact for that channel and would add the app's first heavyweight dependency to a zero-dependency file |
| Search or filter across records | Ubiquitous in the category | No | The group grid holds a handful of name-sorted cards (`1029-1033`). A search box would cost more screen than it saves. The list that needs collapsing is month rows, and the fix there is ordering, not search |

## 6. Prioritized improvement backlog

Buckets: **(i)** land during the React migration, **(ii)** enabled by the new storage layer, **(iii)** later enhancement, **(iv)** explicitly rejected. Items marked (i) that are one-file changes today are called out as ship-now in the prose below the table.

| Item | Problem solved | Impact | Effort | Dependencies | Bucket |
| --- | --- | --- | --- | --- | --- |
| B1 Month rows newest-first, current month expanded and scrolled into view, earlier months collapsed | U1, M11 | High | Medium | none | i |
| B2 `Copy this month` action on the group card | U7 | High | Medium | B1 ordering logic | i |
| B3 Keyboard-operable calendar grid, ARIA grid pattern | A1 | High | Medium | none | i |
| B4 Group cards become real buttons | A2 | High | Low | none | i |
| B5 Closed modals hidden; `role=dialog`, `aria-modal`, name, focus trap, focus restore | A3, A4, A5, A6, A10 | High | Medium | none | i |
| B6 Month in the copy button's accessible name | A9 | Medium | Low | none | i |
| B7 Constrain money and year inputs, clamp in the handler | U5, A17, RP-01 D4 | High | Low | none | i |
| B8 `flex-wrap: wrap` on the calendar controls; remove the summary height clip | M1, M2, M3, M5 | Medium | Low | none | i |
| B9 Group-name prefill becomes a placeholder or is selected on focus | U4 | Medium | Low | none | i |
| B10 Pluralise the two UI count strings and the default-template wording | U6, RP-01 D19 | Medium | Low | Template edit may collide with an existing custom template | i |
| B11 Palette pass: accent and danger darkened, 3:1 focus ring on all controls, real card and day borders, non-colour selection cue | A12, A13, A14, A15 | Medium | Medium | none | i |
| B12 `role=status` on the calendar summary and the copy confirmation | A11 | Low | Low | none | i |
| B13 Name the two textareas, the month select and the year input; fix the orphan labels; add a `main` landmark | A7, A8, A10 | Medium | Low | none | i |
| B14 Restore focus after every save and re-render | A6, U9 | Medium | Low | none | i |
| B15 Disable `Save CSV` and `Clear All Data` at zero groups; add one line of first-run orientation | U10, U11 | Low | Low | none | i |
| B16 Scope the bulk price to the visible month, or confirm a multi-month write with its count | U3 | High | Low | **Blocked by RP-01 open question 3** — is the cross-month write intended | i |
| B17 Wrap the storage layer in `try`/`catch` with a visible, recoverable error | RP-01 D14 | High | Low | none; RP-05 section 6 makes this the first fallback item | ii |
| B18 Versioned JSON export and import covering all three keys | Backup completeness, RP-01 D12, D16, D17 | High | Medium | B17 | ii |
| B19 `Last backup: N days ago` indicator with a nag | The original data loss; U10 | High | Low | B18 | ii |
| B20 `sentAt` per group-month, shown in the month row | U2 | High | Medium | Schema version field from B18 | ii |
| B21 Per-group template override plus named template variants | Section 5 rows 3 and 4 | Medium | Medium | Schema version field from B18 | ii |
| B22 One-step restore of the previous state after a destructive action | No undo; U12 | Medium | Medium | B18 snapshot mechanism | ii |
| B23 Confirm CSV import, stating how many groups will be replaced | U12, RP-01 D11 | Medium | Low | none | ii |
| B24 Remove `beforeunload` | U13, RP-01 D24 | Low | Low | Safe only after B17 and B19 exist | ii |
| B25 Extra merge tokens `{{group}}`, `{{price}}`, `{{dates}}`, plus an unknown-token warning | Section 5 row 3 | Medium | Low | B21 preferred first | iii |
| B26 Weekday-pattern generator across a month range | Section 5 row 1 | Medium | Medium | B3 grid work | iii |
| B27 `.ics` download of a group's dates | Section 5 row 6 | Low | Low | none | iii |
| B28 `navigator.share()` behind a capability check, clipboard as fallback | Section 5 row 7 | Medium on phone only | Low | Verify support on her browser | iii |
| B29 44 px touch targets at phone widths | A18, M7 | Low against AA, Medium as ergonomics | Low | B8 | iii |
| B30 Mark the Ukrainian template block's language | A16 | Low | Low | B21 | iii |
| B31 Online payment collection, automated sending, portals, attendance, PDF, roles, search, full RRULE engine, native app, inbox, notifications | see section 8 | n/a | n/a | n/a | iv |

**Ship-now subset.** B4, B6, B7, B8, B9 and B15 are each a handful of lines in the current `index.html`, carry no schema change, and do not need the migration to start. B7 in particular should not wait: it is the difference between a message reading `PLN 200.00` and one reading `-PLN 50.00`.

**Impact reasoning.** B1, B2 and B20 are High because they attack the only cost that recurs every month and grows with use; measured at three years, the current design puts the one row she needs 2,627 px below the fold on a phone. B17, B18 and B19 are High because they address the incident that started this programme, and RP-05 section 6 already sequences the JSON backup and the `try`/`catch` ahead of any sync. B7 and B16 are High because they are the two paths by which money silently becomes wrong — one visible to students, one invisible to everyone. B3, B4 and B5 are High as *conformance* items and as prerequisites for RP-03's test suite, but Medium in lived cost for a sighted mouse user, and section 7 ranks them accordingly rather than pretending otherwise. B10 is Medium not Low, against RP-01's rating, because the same wording is in the outgoing message.

**Effort reasoning.** Low means a change confined to markup, CSS or one handler with no state-shape change: B4, B6, B7, B8, B9, B10, B12, B13, B14, B15, B16, B17, B19, B25, B27, B28, B29, B30. Medium means new state, new UI, or coordinated changes across render and handlers: B1, B2, B3, B5, B11, B18, B20, B21, B22, B26. Nothing in this backlog is High effort, which is itself a finding — the app is small enough that no worthwhile improvement is expensive. The expensive things are all in section 8.

**Dependency notes.** B16 is the only item blocked by an unresolved product question, RP-01 open question 3: nobody currently knows whether the cross-month price write is intended, and the developer's own prior-art doc asks the same thing. It is therefore excluded from section 11. B20, B21 and B22 all add fields to the persisted shape, which is why they sit behind B18's `schemaVersion` rather than in front of it. B5's status depends on RP-01 open question 1: if the developer's uncommitted `index.html` lands, half of B5 is already done.

## 7. Top three highest-leverage changes, ranked with justification

**1. Make the current month reachable in one action, and mark it once it has been sent.** B1 plus B2 plus B20. This is first because it is the only item whose cost is paid *every month* and *grows monotonically*. Measured at a modelled three years of use, the row she needs is row 36 of 36, requiring 1,381 px of scrolling on a desktop and 2,627 px on a phone, and it is not visible when the modal opens; at five years it is worse. Nothing about the current design gets better with time. In the same breath, `sentAt` answers the question the tool cannot answer today — did August go out — which is the question a billing tool exists to answer. Together these three items convert a six-step monthly chore, three steps of which are pure navigation, into two clicks from the group card.

**2. Stop the app losing her work, and give her a backup she can actually restore from.** B17 plus B18 plus B19. Second, not first, only because the loss is probabilistic while the scrolling cost is certain. But the expected damage is far larger: RP-01 runtime-verified that a single corrupt `localStorage` value produces a silently dead page with no UI route to recovery, and the app's only existing backup path omits the template, has no BOM, throws on a malformed month key it can itself create, and silently destroys existing data on a balanced stray quote. This is also the sequencing RP-05 section 6 already committed to — hardened local storage plus a versioned JSON export ships **before** any Firestore sync — so this item is not a competing proposal, it is the same work seen from the UX side. The backup-age indicator is the part that makes it different from the arrangement that already failed once: a nag she can see beats a discipline she has to remember.

**3. Stop the money going wrong quietly.** B7 plus B16 plus B10. Third because the two failures here are lower-probability than the first two but higher-embarrassment, which is the axis the brief asks me to weigh. Verified: with no `min` or `step` on the price inputs, a mistyped value produces a real outgoing message reading `total fee of -PLN 50.00`; the shipped default template produces `we will have 1 lessons` for every single-lesson month; and one keystroke in the bulk price box rewrote 36 months of prices in the draft with no indication and no undo. The first two are visible to students and parents. The third is visible to nobody, which is worse. B7 and B10 are ship-now; B16 needs one product decision first.

**Why accessibility is fourth, stated plainly.** B3, B4, B5, B11, B12 and B13 fix Level A and AA failures and I am not softening them: the primary task genuinely cannot be completed by keyboard, and 9 of 14 tab stops sit in invisible dialogs. But the sole end user is a sighted teacher operating a mouse, and none of that costs her a minute today. It still belongs in bucket (i) for three reasons that do not depend on her: it is at its cheapest inside a component boundary with real focus management, so doing it during the migration costs a fraction of doing it after; RP-03's Playwright suite cannot write unambiguous role-based queries while 36 buttons share one accessible name and closed dialogs stay in the tree; and a keyboard-operable calendar is the difference between a tool that works on a tablet with a keyboard case and one that does not. Ranking it fourth is a statement about this user's month, not about the work's worth.

## 8. Explicitly rejected ideas with reasons

Each of these is standard in the tutoring-tool category. Each is disproportionate to one teacher, a handful of groups, a static host, a zero-budget constraint and a single maintainer working in personal time.

| Rejected idea | Reason |
| --- | --- |
| Online payment collection, card processing, Stripe [S11] | Requires a merchant relationship, a server-side secret and PCI-adjacent obligations. GitHub Pages has no backend. It would also make the maintainer responsible for other people's money in a personal-time project. She already gets paid by bank transfer to the account named in her own template |
| Automated email or SMS delivery of the message | There is no recipient data in the app at all (zero occurrences of `student`) and no backend to send from. Adding contacts to enable sending would create the app's first store of other people's personal data — the exact category of exposure this programme is already trying to remove from `index.html:387-392` and `400` |
| Parent or student portal with logins | Accounts, password reset, session management and support burden, for an audience that reads a group chat. The clipboard already reaches them |
| Per-student records, attendance, makeup credits, gradebook | Multiplies the data model by the number of students to serve a message that is computed per group per month. The app's arithmetic does not need to know who attended |
| PDF invoice generation | The delivery channel is a chat paste. A PDF is worse there, and it would add the first heavyweight dependency to a file that RP-01 measured at 58,649 bytes with zero dependencies |
| Multi-teacher roles and permissions | There is one teacher. Roles would be pure ceremony |
| In-app two-way messaging inbox | She already has the messaging app her students use. Rebuilding an inbox competes with WhatsApp and Telegram and loses |
| Push notifications and automated reminders | Requires a service worker, a push service and a reason to interrupt her. The monthly cadence is already anchored to a real-world event: the month ending |
| Search box over groups | The grid holds a handful of already name-sorted cards (`index.html:1029-1033`). It would consume more screen than it saves. The list with a real problem is month rows, and the fix is ordering and collapse, not search |
| A full RFC 5545 recurrence engine [S8] | `RRULE` with exception dates, timezones and expansion is a large amount of correctness risk to express a pattern she can paint in seven clicks. Adopt the range-generator (B26) and stop there |
| Subscribable iCal feed | A feed needs a server that can produce per-user output. GitHub Pages serves static files only. A one-off `.ics` download (B27) delivers most of the value with none of the infrastructure |
| Native mobile app | Two store accounts, two release processes and a review queue, to wrap a page that already works in a mobile browser. The mobile problem here is 40 lines of CSS (B8, B29), not a platform |
| SIS or LMS integration | There is no institution on the other end |
| Rich-text template editor | The output is pasted as plain text into a chat. Formatting would be discarded or mangled |
| Full UI internationalisation framework | One user, who reads the English chrome fine. The genuine language problem is narrow — marking the Ukrainian block (B30) and unpinning `en-US` — and does not need an i18n library |
| Analytics or reporting dashboard | Revenue over time is interesting to a business, not to one teacher who already sees each month's total. If she ever wants it, the JSON export (B18) feeds a spreadsheet |
| Real-time multi-device collaboration and conflict resolution | One user, one device at a time. RP-05's single-document last-write-wins model is the correct amount of machinery |
| Automated accessibility remediation via an overlay widget | Overlays do not fix non-focusable `div` controls; the underlying markup has to change. B3 and B4 are the actual fix |

## 9. Risks and unknowns

| Item | Status | What would resolve it |
| --- | --- | --- |
| Which browser, device and screen size she actually uses | `TBD` | Ask her, or read one User-Agent. It changes the severity of M1 to M7 from "phone users are affected" to "she is or is not affected", and it decides whether B28 is worth anything |
| Whether she uses a keyboard, a tablet keyboard case, or any assistive technology | `TBD` | Ask her. It is the only input that could move section 7's fourth-place accessibility ranking |
| Which channel she pastes the message into — Telegram, Viber, WhatsApp, email | `TBD` | Ask her. It decides whether B28's share sheet saves real steps or none |
| `navigator.share()` availability in her browser | `TBD` | Evaluate `'share' in navigator` on her device. MDN states the API is not Baseline [S12], so this cannot be assumed either way |
| Real dataset scale — how many groups and how many months she keeps | `TBD`, inherited from RP-01 | Ask her. Every scale figure in section 4 is from a dataset I constructed (8 groups, 36 months, 2,304 dates) and is labelled a modelled worst case, not an observation of her data |
| Whether the cross-month bulk price write is intended | Open, blocks B16 | RP-01 open question 3. The developer's own prior-art notes ask the same question, so this is a product decision, not a code question |
| Whether the developer's uncommitted `index.html` will land | Open, changes A3's status | RP-01 open question 1 |
| Whether editing the default template's plural wording is safe for her specifically | Open | She may already have a custom `paymentTemplate`, in which case the default is never read on her machine and B10's template half is a no-op for her. Inspect whether that key exists on her device before assuming impact |

**Contradictions with inputs, stated explicitly.**

1. **Target size.** RP-01 section 6 records target size as *passing* WCAG 2.2 SC 2.5.8 at 24x24. This report records it as *failing* WCAG 2.1 SC 2.5.5 at 44x44 [S1]. Both are correct; they are different criteria in different versions, and 2.5.8 does not exist in 2.1. Any later report must name the version before quoting either verdict.
2. **The clipped calendar controls at 375 px.** RP-01 says both `◀` and `Clear Month` "are unreachable without scrolling inside the modal". Measured here, `◀` is unreachable **even with** scrolling, because `modal.scrollLeft` cannot go below 0 while `justify-content: center` pushes content off the left edge. Prefer this report on that point.
3. **The plural defect's severity.** RP-01 rates D19 Low. The identical wording is in the shipped default template at `index.html:384` and appears in the message students read. Medium is the right rating.
4. **`navigator.storage.estimate()` as a quota answer.** RP-01 proposed it as the way to resolve the localStorage quota TBD. It does not resolve it: it returned 10,737,418,240 bytes of quota with usage 0 while `localStorage` held data, so **no later report should cite 10 GiB as the localStorage ceiling**. The direct `setItem` probe does resolve it, at 5,177,344 characters in one key. Carry the unit caveat with the number: that is 4.94 MiB of characters or 9.88 MiB of UTF-16 bytes, and MDN's per-origin figure does not state which basis it uses [S7]. Reading the Chromium `DOMStorage` quota implementation would settle the basis; nothing in this programme depends on the answer, because the real dataset is three orders of magnitude smaller.

Nothing in this report blocks a later report. B16 is the only backlog item gated on an open decision, and it is excluded from section 11 accordingly.

## 10. Sources

| # | Title | URL | Accessed | Supports |
| --- | --- | --- | --- | --- |
| S1 | Web Content Accessibility Guidelines (WCAG) 2.1, W3C Recommendation | https://www.w3.org/TR/WCAG21/ | 2026-08-20 | Every criterion number and conformance level used in section 3; the 44x44 threshold and Level AAA status of 2.5.5 Target Size; confirmation that 2.4.11, 2.5.7, 2.5.8, 3.2.6, 3.3.7 and 3.3.8 do not exist in WCAG 2.1 |
| S2 | Understanding SC 1.4.3 Contrast (Minimum) | https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html | 2026-08-20 | The 4.5:1 and 3:1 thresholds, Level AA, and the large-text definition of 18 pt or 14 pt bold with the 1 pt = 1.333 px conversion used to rule the 13.33 px and 16 px cases as normal text |
| S3 | Understanding SC 1.4.11 Non-text Contrast | https://www.w3.org/WAI/WCAG21/Understanding/non-text-contrast.html | 2026-08-20 | The 3:1 against-adjacent-colour rule, and the explicit inclusion of focus indicators and state indicators, which is what makes the 2.78:1 focus ring and the 2.66:1 selected-day state failures |
| S4 | ARIA Authoring Practices Guide, Grid pattern | https://www.w3.org/WAI/ARIA/apg/patterns/grid/ | 2026-08-20 | The single-tab-stop plus arrow-key model and the grid, row and gridcell roles proposed as the fix for the non-focusable calendar |
| S5 | ARIA Authoring Practices Guide, Modal Dialog pattern | https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/ | 2026-08-20 | Focus placement on open, Tab containment, Escape, focus restoration, and the role, aria-modal and labelling requirements behind findings A4, A5 and A6 |
| S6 | Understanding SC 4.1.3 Status Messages | https://www.w3.org/WAI/WCAG21/Understanding/status-messages.html | 2026-08-20 | Level AA, what counts as a status message, the role=status and aria-live techniques, and the exclusion of focus-taking dialogs that puts the alert() calls outside scope |
| S7 | MDN, Storage quotas and eviction criteria | https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria | 2026-08-20 | The documented 10 MiB Web Storage maximum, the 5 MiB of localStorage per origin, and the QuotaExceededError on overflow. Cited with the caveat that the source does not state whether those figures count characters or bytes, which is why M9 reports the measurement in both units |
| S8 | RFC 5545, Internet Calendaring and Scheduling Core Object Specification (iCalendar) | https://www.rfc-editor.org/rfc/rfc5545.html | 2026-08-20 | RRULE with FREQ and BYDAY as the standard encoding of a weekly pattern, and VEVENT as the event component, used in the recurrence and .ics rows of section 5 |
| S9 | Google Workspace Help, Send a personalized email to multiple people (mail merge) | https://support.google.com/mail/answer/12921167 | 2026-08-20 | The documented merge-tag set and spreadsheet-column tags used as the comparison point for the app's three-token template |
| S10 | Teachworks product documentation, Scheduling and Calendar | https://www.teachworks.com/features/calendar | 2026-08-20 | Documented existence of repeating lessons, group lessons, calendar feeds and lesson reminders in the tutoring-tool landscape. Used for capability existence only |
| S11 | Teachworks product documentation, Billing and Invoicing | https://www.teachworks.com/features/billing | 2026-08-20 | Documented existence of scheduled automatic invoice generation, transaction history, per-scenario email templates, overdue reminders, PDF invoices and Stripe payments. Used for capability existence only |
| S12 | MDN, Navigator.share() | https://developer.mozilla.org/en-US/docs/Web/API/Navigator/share | 2026-08-20 | The secure-context and transient-activation requirements, the text field, and the explicit statement that the feature is not Baseline, which is why B28 is a capability-checked enhancement rather than a replacement |

Two source-handling notes. S10 and S11 are vendor pages; they are cited **only** as evidence that a capability exists in the category, never for any claim about quality, and every adopt or reject verdict in section 5 is justified against a cost measured in this application rather than against the vendor's framing. RP-01 and RP-05 are inputs to this report, not sources, and are referenced by section rather than by a source row.

## 11. Quick wins

Every item below is user-visible, small, low-regression, and blocked by no unresolved question in this report. Deliberately excluded: scoping the bulk price write (blocked by RP-01 open question 3), the storage `try`/`catch` and JSON backup (owned by RP-05 and larger than a quick win), and the six wins RP-01 already issued, none of which is repeated here.

One boundary to note, because backlog item B7 bundles them. Quick win 2 below covers the **price** inputs only. Constraining the **year** input is a different failure with a different fix and is already covered by RP-01's own quick win, so it is not restated here; B7 should be read as those two changes together.

| Rank | Quick win | Effort | Impact | Basis of ranking |
| --- | --- | --- | --- | --- |
| 1 | Sort month rows newest-first | XS | High | Measured 1,381 px of desktop and 2,627 px of phone scrolling to reach the row she needs every month, purely because of an ascending sort at one line. One comparator changes it. Highest impact-to-effort ratio in the report |
| 2 | Constrain the two price inputs and clamp negatives | XS | High | Stops a verified outgoing message reading a negative total. Three attributes plus one clamp; purely additive validation with no behaviour change for valid input |
| 3 | Let the calendar controls wrap, and unclip the summary | XS | Medium | Fixes the only permanently unreachable control in the app, measured at 16.2 px of 35.3 px visible with `scrollLeft` pinned at 0, plus a two-pixel text clip at every width. Two CSS declarations |
| 4 | Make group cards keyboard-operable | S | Medium | Restores the only keyboard route to open an existing group, which is the return-visit entry point to the whole product. Three lines, no state change |
| 5 | Put the month in the copy button's accessible name | XS | Medium | Turns 36 identical accessible names into 36 distinct ones, and unblocks RP-03's role-based queries before the suite is written |
| 6 | Stop the group-name prefill colliding with typing | XS | Medium | Every new group currently requires deleting 14 characters first, verified as producing `Untitled GroupB2 Adults`. One call or one attribute change |

```text
PROMPT QW-1: Show the most recent month first in the group modal
Context: Repo lesson-planner, single deployed file index.html at the repo root, served by GitHub Pages. App.render.monthlyOverrides at index.html:1055-1078 builds the month rows inside the group modal. Line 1067 reads Array.from(monthsToRender).sort().forEach(monthKey => ...), which sorts YYYY-MM keys as strings in ascending order, so the oldest month renders first and the current month renders last. Measured on a seeded dataset of one group with 36 consecutive months of lessons: the current month is row 36 of 36, the modal content is 2282px tall in an 850px box at 1280x900 and 3453px in a 771px box at 375x812, and reaching the current month requires 1381px of scrolling on the desktop viewport and 2627px on the phone viewport. The current month is not visible when the modal opens.
Task: Reverse the ordering at index.html:1067 so month rows render newest-first, by replacing the bare sort() with a comparator that sorts the YYYY-MM keys in descending order. Change nothing else about how the rows are built.
Constraints: Do not change the row markup produced by App.render.createMonthRow at index.html:1080-1124, including the month-name string, the Total and Per lesson strings, and the copy-msg-btn label. Do not change which months are selected for rendering at index.html:1063-1066, so the current calendar month still always gets a row. Do not change the three localStorage key names groupLessonPlannerData, groupLessonPlannerSettings and paymentTemplate, and do not change the persisted data shape. Do not change the group-card sort at index.html:1029-1033. No new dependencies, no build step. Out of scope: collapsing past months and scrolling the current month into view, which are separate larger changes.
Acceptance criteria: With a group holding lessons in three or more distinct months, the first .month-override-row in the DOM is the latest month key and the last is the earliest. The current calendar month's row is within the first two rows whenever the current month has lessons or is the visible calendar month. Every month that rendered before the change still renders after it, with the same lesson count, the same Total and the same Per lesson value. Clicking a month row still opens the schedule editor on that month via App.handlers.startEditingDates. The per-month price input still updates only its own row's total while the schedule editor is open.
Verification: Serve the file locally, seed groupLessonPlannerData with one group whose monthlyOverrides contains at least the keys 2024-09, 2025-03 and 2026-08 each with dates, reload, open the group, then read the text of every .month-override-row .month-name in DOM order and assert the sequence is August 2026, March 2025, September 2024. Then assert the number of rows equals the number before the change for the same seed.
```

```text
PROMPT QW-2: Prevent a negative or sub-cent price from reaching the payment message
Context: Repo lesson-planner, index.html. The default price input at index.html:287 and the bulk price input at index.html:332 are both type=number with no min and no step; runtime inspection confirmed both attributes resolve to the empty string. App.handlers.updateDefaultPrice at index.html:690-705 and App.handlers.handleSelectedDatesPriceChange at index.html:858-881 both accept the parsed value with no range check, and the per-month input handler at index.html:1110-1115 does the same. Verified consequence: a price of -50 with one lesson produces a generated payment message whose fee line reads a negative currency amount, and App.utils.formatCurrency at index.html:1379-1381 formats it without complaint. That message is what the teacher copies and sends to students.
Task: Add min="0" and step="0.01" to the price inputs at index.html:287 and index.html:332, and add the same two attributes to the per-month price input created at index.html:1094. Then clamp the parsed value to a minimum of 0 in all three handlers that read those inputs, so a negative typed value is treated as 0 rather than stored.
Constraints: Do not change the existing Number(...) || 0 fallback semantics for blank or non-numeric input, which must continue to yield 0. Do not add a maximum. Do not change App.utils.formatCurrency. Do not change the three localStorage key names groupLessonPlannerData, groupLessonPlannerSettings and paymentTemplate, and do not change the persisted data shape; this prompt prevents new bad values and deliberately does not migrate existing ones. Do not change which months the bulk price handler writes to, which is a separate open product question. No new dependencies.
Acceptance criteria: Typing -50 into the default price input and committing leaves group.price at 0, and the group info display shows a zero amount rather than a negative one. Typing -50 into the bulk price input leaves every affected month override price at 0. Typing -50 into a per-month price input leaves that month's price at 0 and its total at 0. A valid price such as 200 still round-trips unchanged, and a decimal price such as 12.50 is still accepted. A generated payment message never contains a minus sign in the fee amount.
Verification: Serve locally, add a group, open it, type -50 into Default Price, blur, and read App.state.groups[0].price and the on-screen Default Price value. Repeat with the bulk price input while dates are selected and read every price in App.state.tempMonthlyOverrides. Repeat with a per-month price input and read the row's Total text. Finally generate a payment message for a month with one lesson and assert the fee amount contains no minus sign.
```

```text
PROMPT QW-3: Let the calendar control row wrap, and stop the calendar summary clipping its text
Context: Repo lesson-planner, index.html. The .calendar-controls rule at index.html:179 sets display:flex with justify-content:center and no flex-wrap, so it computes flex-wrap: nowrap. Measured at a 375x812 viewport with the schedule editor open: the control row has scrollWidth 364 against clientWidth 315; the previous-month button at index.html:318 renders at x = -19.1 with width 35.3, leaving 16.2px on screen; Clear Month at index.html:323 extends to x = 394.1 in a 375px viewport; and the enclosing .modal has scrollWidth 394 against clientWidth 375. Setting the modal's scrollLeft to -9999 leaves it at 0, so the clipped left portion of the previous-month button cannot be brought into view by scrolling at all. Separately, the #calendar-summary rule at index.html:194-200 sets height: 1em, which measures clientHeight 12 against scrollHeight 14 at both 375px and 768px, clipping the text.
Task: Add flex-wrap: wrap to the .calendar-controls rule at index.html:179 so the six controls wrap onto additional rows at narrow widths instead of overflowing. In the #calendar-summary rule at index.html:194-200, replace height: 1em with min-height: 1em so the summary can grow to fit its own text.
Constraints: Do not change the markup order of the six calendar controls at index.html:317-324, and do not change their ids. Keep justify-content: center. Do not change the calendar grid rules at index.html:175-178 or the .calendar-dow rules at index.html:181-190. Do not change the three localStorage key names or the persisted data shape. No new dependencies and no media queries; a single flex-wrap declaration is sufficient. Out of scope: increasing touch target sizes, which is tracked separately.
Acceptance criteria: At a 375px viewport with the schedule editor open, every one of the six calendar controls has a bounding rectangle whose left edge is at or above 0 and whose right edge is at or below the viewport width. The enclosing .modal reports scrollWidth equal to clientWidth, so no horizontal scrollbar appears inside the dialog. At a 1280px viewport the control row still renders on a single centred line with the same visual order. #calendar-summary reports scrollHeight equal to clientHeight at 375px, 768px and 1280px, and its text is fully visible.
Verification: Serve locally, seed one group with dates, open the group, open the schedule editor, set the viewport to 375x812 and read getBoundingClientRect for each of prevMonthBtn, monthSelect, yearInput, nextMonthBtn, todayBtn and clearMonthBtn, asserting 0 <= left and right <= 375. Assert the .modal scrollWidth equals its clientWidth. Then select one date and assert #calendar-summary scrollHeight equals clientHeight. Repeat the summary assertion at 768x1024 and 1280x900, and confirm the control row is a single line at 1280px.
```

```text
PROMPT QW-4: Make group cards operable from the keyboard
Context: Repo lesson-planner, index.html. App.render.createGroupCard at index.html:1041-1053 builds each card as a plain div with class group-card and an onclick handler that calls App.handlers.openGroupModal(index). Runtime inspection confirmed the card computes tabIndex -1, has no role attribute and has no key handler. A full 16-step Tab sweep on a page with one card present never reaches the card, so once the group modal has been closed there is no keyboard route to open any group again. This is the return-visit entry point to the entire product.
Task: In App.render.createGroupCard, make the card keyboard operable by adding tabindex="0", role="button" and a keydown handler that calls the same App.handlers.openGroupModal(index) when the pressed key is Enter or the space character, calling preventDefault for the space case so the page does not scroll. Give the card an accessible name that includes the group name.
Constraints: Do not change the card's visual appearance, its class name group-card, or the .group-card CSS at index.html:23-46. Do not change the existing onclick handler; the keydown handler is additive so mouse behaviour is untouched. Do not change the h2 and .group-card-info markup inside the card at index.html:1046-1051, and do not change the "N planned lessons" wording. Do not change the three localStorage key names or the persisted data shape. Do not add a focus-ring style in this change; the browser default is acceptable here and the palette work is tracked separately. No new dependencies.
Acceptance criteria: On a page with three group cards and no modal open, tabbing from the document start reaches all three cards in the order they appear in the DOM, before or after the toolbar buttons but never skipped. Pressing Enter on a focused card opens the group modal for that specific card's group. Pressing the space bar on a focused card opens the same modal and does not scroll the page. Clicking a card with the mouse still opens the correct group. Each card exposes an accessible name that contains its group name. Deleting a group and re-rendering still produces focusable cards for the remaining groups.
Verification: Serve locally, seed three groups with distinct names, reload, then Tab from the document start recording document.activeElement for each stop and assert all three .group-card elements appear. Focus the second card, press Enter, and assert App.state.editingIndex matches that card's group and the modal has the show class. Close the modal, focus the third card, press the space bar, and assert the correct group opened and window.scrollY is unchanged. Finally click the first card with the mouse and assert it still opens.
```

```text
PROMPT QW-5: Name each copy button after the month it copies
Context: Repo lesson-planner, index.html. App.render.createMonthRow at index.html:1080-1124 renders one button per month row with the literal label "📋 Copy Payment Message" at index.html:1099. The month name is in a sibling div at index.html:1089 and is not programmatically associated with the button. Measured on a seeded dataset of one group with 36 months of lessons: 36 buttons in the same dialog share the single distinct accessible name "📋 Copy Payment Message". The same document also exposes four buttons named Cancel and two named Save. This makes role-and-name queries ambiguous for the Playwright suite planned in RP-03, and gives a screen-reader user 36 indistinguishable controls.
Task: In App.render.createMonthRow, add an aria-label to the copy button at index.html:1099 that includes the month name and year already computed at index.html:1084 and 1083, for example a label of the form Copy payment message for August 2026. Keep the visible button text exactly as it is.
Constraints: Do not change the visible label text "📋 Copy Payment Message", the copy-msg-btn class name, or the disabled attribute logic that disables the button when lessonCount is 0. Do not change the month-name string at index.html:1089 or the Total and Per lesson strings. Do not change the click handler at index.html:1118-1122 or the message generated by App.utils.generateMonthlyPaymentMessage. Do not change the three localStorage key names or the persisted data shape. No new dependencies. Note that the month name comes from App.config.monthNames at index.html:379 and must not be re-derived some other way.
Acceptance criteria: With a group holding lessons in three or more distinct months, every .copy-msg-btn in the dialog has a distinct accessible name, and each name contains that row's month name and four-digit year exactly as the row's own heading shows them. The visible text of every such button is still "📋 Copy Payment Message". A month with zero lessons still renders a disabled button. Clicking any button still opens the review modal with the message for that month and no other.
Verification: Serve locally, seed one group with monthlyOverrides for at least 2024-09, 2025-03 and 2026-08 each holding dates, reload, open the group, then collect the aria-label of every .copy-msg-btn and assert the set has the same size as the number of rows and that each label contains the matching month name and year. Assert every button's textContent still contains Copy Payment Message. Click the button in the 2025-03 row and assert the review textarea content corresponds to that month's lesson count.
```

```text
PROMPT QW-6: Stop the Untitled Group prefill colliding with what the user types
Context: Repo lesson-planner, index.html. App.handlers.openGroupModal at index.html:603-637 sets groupNameInput.value to the literal string "Untitled Group" at index.html:614 in add mode, then focuses that input at index.html:635 after a 100ms timeout. Runtime inspection confirmed the input's selectionStart and selectionEnd are both 14 when focus lands, so nothing is selected and the caret sits at the end. Typing B2 Adults immediately after opening the dialog produced the value "Untitled GroupB2 Adults". Every new group therefore requires the user to delete 14 characters before typing. App.handlers.saveGroup at index.html:661 already falls back to 'Untitled Group' when the trimmed name is empty, so the prefill is not needed to guarantee a non-empty name.
Task: In add mode, stop prefilling the group name with a literal value. Set groupNameInput.value to an empty string at index.html:614 and add a placeholder of Untitled Group to the input at index.html:282 so the fallback name is still visible to the user. Leave the focus call at index.html:635 as it is.
Constraints: Do not change App.handlers.saveGroup's empty-name fallback at index.html:661, which must still produce 'Untitled Group' when the field is left blank. Do not change the different fallback 'Untitled' at index.html:677 used on edit, which is out of scope. Do not change edit mode, where groupNameInput.value is correctly set from the existing group at index.html:623. Note that App.render.groupInfo at index.html:997 and 1008 reads groupNameInput.value when no group exists yet and already falls back to 'Untitled Group', so verify the add-mode display still shows a sensible name with the field empty. Do not change the three localStorage key names or the persisted data shape. No new dependencies.
Acceptance criteria: Clicking + Add Group leaves the group name field empty with a visible placeholder reading Untitled Group. Typing B2 Adults immediately after the dialog opens produces exactly the value B2 Adults. Clicking Save with the field left untouched creates a group named Untitled Group. The read-only group name display shown while adding does not render an empty string or the word undefined. Opening an existing group still prefills its real name and still allows editing it.
Verification: Serve locally with empty localStorage, click + Add Group, assert groupNameInput.value is the empty string and its placeholder attribute is Untitled Group, then type B2 Adults and assert the value is exactly B2 Adults. Reload, click + Add Group, click Save without typing, and assert App.state.groups[0].name is Untitled Group. Reload again, open the existing group, and assert groupNameInput.value equals the stored name.
```
